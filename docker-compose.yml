version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./generated_images:/app/generated_images
    environment:
      - FLASK_ENV=production
    restart: always